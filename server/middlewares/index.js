const notFoundUrl = require("./notFoundUrl");
const errorHandler = require("./errorHandler");
const validateBody = require("./validateBody");

module.exports = {
  notFoundUrl,
  errorHandler,
  validateBody,
};
