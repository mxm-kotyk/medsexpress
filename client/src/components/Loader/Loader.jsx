import { LoaderWrapper, StyledLoader } from "./Loader.styled";

const Loader = () => {
  return (
    <LoaderWrapper>
      <StyledLoader></StyledLoader>
    </LoaderWrapper>
  );
};

export default Loader;
